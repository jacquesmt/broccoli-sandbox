{
  "name": "broccoli-demo",
  "version": "1.0.0",
  "description": "sample broccoli test",
  "main": "index.js",
  "scripts": {
    "build": "rm -rf dist && broccoli build dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "broccoli": "^1.1.4",
    "broccoli-funnel": "^2.0.1",
    "broccoli-merge-trees": "^2.0.0"
  }
}
